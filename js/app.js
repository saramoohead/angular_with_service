var githubUserSearch = angular.module('GitUserSearch', ['ngResource']);
// an angular module is a collection services, directives, controllers and
// angular.module is used to configure the $injector
// ngResource is for interacting with RESTful services via that $resource service

